export default function Game({ gameState, setGameState }: { gameState: gameStateType, setGameState: React.Dispatch<React.SetStateAction<{ status: string }>>   }) {

  return (
    <div>Game</div>
  )
}
